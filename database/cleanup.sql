SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE IF EXISTS cache, cache_locks, clients, employees, failed_jobs, job_batches, jobs, migrations, password_reset_tokens, payrolls, sessions, users, attendance;
SET FOREIGN_KEY_CHECKS = 1;
